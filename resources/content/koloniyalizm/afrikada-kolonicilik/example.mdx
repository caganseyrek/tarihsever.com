# Example

import ContentSubheader from "@/components/content-subheader/ContentSubheader";

<ContentSubheader
  breadcrumbs={["Koloniyalizm", "Afrika'da Kolonicilik", "Example"]}
  pagePath="konular/koloniyalizm/afrikada-kolonicilik/example"
/>

TODO automate adding component above. maybe extract the title, push the component then put the title back

component above are only for the articles, not the pages
